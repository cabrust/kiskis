[build-system]
requires = ["setuptools >= 77.0.3"]
build-backend = "setuptools.build_meta"

[project]
name = "kiskis"
version = "0.0.2"

dependencies = [
  "flask==3.1.3",
  "PyYAML==6.0.3",
  "cryptography==48.0.0"
]

[project.scripts]
kiskis = "kiskis.main:main"