[build-system]
requires = ["setuptools >= 77.0.3"]
build-backend = "setuptools.build_meta"

[project]
name = "kiskis"
version = "0.0.1"

dependencies = [
  "flask==3.1.3",
  "PyYAML==6.0.3"
]

[project.scripts]
kiskis = "kiskis.main:main"